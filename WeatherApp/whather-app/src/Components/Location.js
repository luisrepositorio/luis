import React from 'react';

Location = ({ city }) => (
    <div>
        <h1>
            {city}
        </h1>
    </div>
);
    //Location = (props) => {
    //const city = props.city;
    //Destructuring
    //const { city } = props;
   

   

export default Location;
